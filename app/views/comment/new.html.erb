<h1>new page</h1>
 <%#=form_for @comment, url: {action: "create"} do |f| %>
 <%#=form_with(model: [@blog, @post, @comment], remote: true) do |form| %>
 <%=form_for @comment, url: "#{@comment.post_id}/comment", remote: true do |f| %>
 <%#= form_with(model: [@blog, @post, @comment]) do |f| %>
 <%# form_for(@comment, :url => blog_post_comments_path, :html => {:method => :post}) do |f| %>
 <%#=form_for @comment, url: blog_post_comments_path, remote: true do |f| %>
 <%#= form_with(model: [@blog, @post, @comment], remote: true) do |form| %>
 <p>
   <%= f.label :commitment %><br>
   <%= f.text_area :commitment %>
 </p>
 <p>
   <%= f.submit %>
 </p>
<% end %>
